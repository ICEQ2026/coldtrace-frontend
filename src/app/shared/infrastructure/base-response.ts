/**
 * @summary Marker interface for response envelopes returned by API endpoints.
 */
export interface BaseResponse {}

/**
 * @summary Base resource contract exchanged with API endpoints.
 */
export interface BaseResource {
  id: number;
}
