import { TemperaturePoint } from './temperature-pointentity';

describe('TemperaturePoint', () => {
  it('should create an instance', () => {
    expect(new TemperaturePoint()).toBeTruthy();
  });
});
