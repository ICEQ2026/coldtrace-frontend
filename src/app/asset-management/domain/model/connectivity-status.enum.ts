/**
 * @summary Defines the allowed connectivity status values used by the asset management bounded context.
 */
export enum ConnectivityStatus {
  Online = 'online',
  Unstable = 'unstable',
  Offline = 'offline',
}
