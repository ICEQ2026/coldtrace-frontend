/**
 * @summary Defines the allowed IoT device status values used by the asset management bounded context.
 */
export enum IoTDeviceStatus {
  Available = 'available',
  Linked = 'linked',
  Offline = 'offline',
}
