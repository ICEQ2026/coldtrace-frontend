/**
 * @summary Defines the allowed gateway status values used by the asset management bounded context.
 */
export enum GatewayStatus {
  Active = 'active',
  Maintenance = 'maintenance',
  Offline = 'offline',
}
