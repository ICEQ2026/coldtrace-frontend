/**
 * @summary Defines the allowed asset status values used by the asset management bounded context.
 */
export enum AssetStatus {
  Active = 'active',
  Maintenance = 'maintenance',
  Inactive = 'inactive',
}
