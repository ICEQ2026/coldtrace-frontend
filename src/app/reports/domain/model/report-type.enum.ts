/**
 * @summary Defines the allowed report type values used by the reports bounded context.
 */
export enum ReportType {
  DailyLog = 'daily-log',
  Compliance = 'compliance',
  MonthlySummary = 'monthly-summary',
}
